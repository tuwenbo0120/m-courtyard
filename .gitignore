# ── OS ──
.DS_Store
Thumbs.db

# ── Node / Frontend ──
app/node_modules/
app/dist/
app/dist-ssr/
app/*.local
app/pnpm-debug.log*

# ── Rust / Tauri ──
app/src-tauri/target/

# ── Editor ──
.vscode/
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Python ──
__pycache__/
*.pyc
*.pyo
.env

# ── Build artifacts ──
*.dmg
*.app
*.deb
*.AppImage
*.msi

# ── User data (runtime) ──
# ~/Courtyard/ is the runtime data dir, not in repo

# ── Old git history backup ──
.git_backup/
.git-backup/
app/.git_backup/
app/.git-backup/

# ── Internal docs & history (not for public repo) ──
docs/*
!docs/screenshots/
userimg/
history/

# ── Misc ──
*.log
logs/
test/
