# Courtyard 测试文档（Bug 跟踪）

- 产品：Courtyard - AI Model Training Assistant
- 版本：0.5.5
- 测试时间：2026-04-21 23:45 (UTC+08)
- 测试方式：代码走查 + 发布前版本核对
- 测试环境：macOS / Apple Silicon

## Bug 清单

| Bug编号 | 模块 | 描述 | 状态 | 修改时间 |
| --- | --- | --- | --- | --- |
| BUG-166 | 在线模型/发布 | 在线模型推荐已按当前主流本地可用系列更新；移除 Phi / Mistral，补充 Gemma，并同步 0.5.5 发布版本信息 | 已修复 | 2026-04-21 |
| BUG-165 | 环境初始化/训练/导出 | 升级到 v0.5.3 后会识别并拦截过旧的 mlx-lm 版本，避免 Gemma 4 训练/导出继续报错 `Model type gemma4 not supported` | 已修复 | 2026-04-12 |
| BUG-164 | 在线模型/训练/导出 | 在线模型列表已更新为最新本地可用版本，Gemma 4 已支持本地扫描、训练与导出，GLM 已同步更新 | 已修复 | 2026-04-10 |
| BUG-163 | 环境初始化 | macOS 环境初始化已移除不兼容的 `--system-certs` 参数，改为 `UV_SYSTEM_CERTS=true` | 已修复 | 2026-04-10 |
| BUG-162 | 环境初始化 | 企业网络 TLS 拦截问题：已将 --native-tls（已废弃）升级为官方推荐 --system-certs，同时注入 UV_SYSTEM_CERTS=true；新增 build_uv_env() 统一为所有 uv 子进程注入代理、SSL 证书和 shell 环境变量继承（含 SSL_CERT_FILE / SSL_CERT_DIR） | 已修复 | 2026-03-25 |
| FEAT-N1 | 设置页 | 集群十一企业网络兼容：设置页新增「网络与代理」区块，支持 HTTP/HTTPS 代理、SSL 证书文件/目录配置；自动继承 shell profile 代理变量；i18n 中英双语 | 已实现 | 2026-03-25 |
| BUG-161 | 训练历史/图表 | 对比图在一条记录无 loss 数据时仍可正常显示另一条记录曲线 | 已修复 | 2026-03-23 |
| BUG-160 | 训练历史/图表 | 历史记录与对比图的 loss 图表样式已统一为与训练页一致 | 已修复 | 2026-03-23 |
| BUG-159 | 训练历史/图表 | 历史记录 loss 曲线已可正常写入并显示，不再出现有坐标轴无线条问题 | 已修复 | 2026-03-23 |
| BUG-158 | 训练历史 | 历史记录在训练中途显示"已停止"；根因：训练开始时 adapter 目录已创建但 training_result.json 尚未写入，fallback 返回"stopped"；修复：TrainingHistory.tsx 在 displayRecords 中过滤掉当前 runningAdapterPath（status=running 时），训练中的记录不出现在历史列表 | 已修复 | 2026-03-21 |
| BUG-157 | 训练历史 | 训练完成后历史记录无 Loss 数据；根因：保存逻辑在 React useEffect 中存在 stale closure 风险，且历史列表不会因训练完成而自动刷新；修复：将保存逻辑从 useEffect 完全移入 trainingStore.ts 的 training-complete 事件监听器内，直接用 get() 读取最新数据（trainLossData/valLossData/adapterPath/startedAt/currentIter），保存成功后自动调用 loadHistory 刷新列表；TrainingPage.tsx 的旧保存 useEffect 改为纯刷新历史 | 已修复 | 2026-03-21 |
| BUG-156 | 训练历史 | 停止训练后历史列表不刷新；修复：handleStop 成功后追加 loadHistory 调用 | 已修复 | 2026-03-21 |
| BUG-155 | 训练历史 | 中途停止训练后状态显示"未知"；根因同 BUG-155（training_result.json 未写入）；随 BUG-155 一并修复 | 已修复 | 2026-03-21 |
| BUG-155 | 训练历史 | 中途停止训练后状态显示"未知"；根因：adapterPath 仅在日志中出现"Saved...to..."行时才赋值，提前停止时为 null，导致 training_result.json 未写入，Rust fallback 返回"unknown"；修复：start_training 返回 {job_id, adapter_path} 结构体，前端训练开始时立即存入 store；Rust fallback 从 unknown 改为 stopped | 已修复 | 2026-03-21 |
| BUG-154 | 训练历史 | 中途停止训练后 Loss 曲线显示"无数据"；根因同 BUG-155（training_result.json 未写入）；随 BUG-155 一并修复 | 已修复 | 2026-03-21 |
| BUG-153 | 训练历史 | 历史记录缺少批量删除功能；修复：每条卡片左侧新增独立复选框（方形，区别于圆形对比按钮），勾选任意条后顶部出现批量操作 toolbar（已选 N 条 / 全选 / 删除所选），删除需二次确认（toolbar 内展示确认文字 + 全部删除按钮），删除完成后自动刷新列表；i18n 中英双语同步 | 已修复 | 2026-03-21 |
| BUG-152 | 数据准备/LM Studio | 检测连接按钮无反馈；修复：新增 lmsCheckResult state（idle/connected/failed），检测成功后按钮变为绿色“已连接”，失败后变为橙色“未连接”，4 秒自动重置；同时移除绿色提示框，改为“使用 LM Studio 本地服务生成...”右侧的 Info 图标悬浮提示层 | 已修复 | 2026-03-17 |
| BUG-151 | 数据准备/LM Studio | LM Studio 数据生成报 502 Bad Gateway；根因：LM Studio 聊天界面可用 ≠ API 服务器已启动，用户需在 Developer 标签页手动开启 Start Server；原脚本直接发 chat/completions 请求得到模糊错误；修复：加 preflight GET /v1/models 检测，区分“服务器未启动”“服务器已启动但未加载模型”“模型找不到”三种情况，错误消息包含精准步骤式指引（Developer → Local Server → Status 开关切为 Running 或按 ⌘R，确认底部显示 Reachable at）；新增 gen.lms_server_ok/lms_server_not_running/lms_no_models 三个 i18n key（zh-CN + en） | 已修复 | 2026-03-17 |
| BUG-150 | 数据准备/LM Studio | 选择 LM Studio 生成方式后，日志仍显示“连接 Ollama...”和“Ollama 连接失败”；根因：generate_dataset_lmstudio.py 与 Ollama 脚本共用 gen.connecting/gen.connect_ok/gen.connect_fail 等 i18n key，这些 key 硬编码了"Ollama"品牌；且 LM Studio 专属 error key（gen.lmstudio_model_not_found/gen.lmstudio_connect_error）未在 i18n locale 中定义；修复：新增 gen.lms_connecting/lms_connect_ok/lms_connect_fail/lms_connect_error/lms_model_not_found 五个 LM Studio 专属 key（zh-CN + en），脚本改用专属 key，Ollama 脚本和 i18n 完全未修改 | 已修复 | 2026-03-17 |
| BUG-149 | 数据准备/LM Studio | scan_lmstudio_models 扫描路径错误（使用 ~/.lmstudio/models 但实际 LM Studio 2.x 存储在 ~/.lmstudio/hub/models/），且检测逻辑仅查找 .gguf/.safetensors 而 LM Studio hub 格式只有 manifest.json/model.yaml；修复：探测三条候选路径（configured/configured+hub/models/parent+hub/models），接受含 manifest.json 或 model.yaml 的目录为有效模型 | 已修复 | 2026-03-17 |
| BUG-148 | 导出页 | 4.4 折叠区内仅显示"导出到 Ollama"，LM Studio(MLX) 和 GGUF 作为独立卡片显示在 4.4 框外，与用户认知不符；修复：将三个导出按钮均移入 step4Open 折叠区，进度/结果区移入 section 内（随时可见） | 已修复 | 2026-03-17 |
| BUG-147 | 导出页 | Ollama 导出区底部含"同时保留 MLX 模型（keepFused）"checkbox，逻辑上 LM Studio(MLX) 导出已独立，该 checkbox 冗余且令人困惑；移除 keepFused state 及相关 UI | 已修复 | 2026-03-17 |
| BUG-146 | 消息提醒 | production .app 通知完全消失（既无弹窗也无声音）；根因：macOS Finder 启动的 .app 的 PATH 不含 `/usr/bin`，导致 `Command::new("osascript")` 和 `Command::new("afplay")` 找不到可执行文件，`?` 提前返回，声音也被跳过；修复：改用全路径 `/usr/bin/osascript` 和 `/usr/bin/afplay`，同时将声音播放提前（与通知解耦，通知失败也播声音），移除 `notify-rust` 的发送路径（dev + production 统一用 osascript）；移除 `notify-rust` 依赖彻底清理 | 已修复 | 2026-03-11 |
| BUG-145 | 消息提醒 | 通知声音 Ping 体验不佳；改为 macOS 内置 Glass.aiff（清脆提示音，适合任务完成场景）；`DEFAULT_NATIVE_NOTIFICATION_SOUND` 常量同步更新 | 已修复 | 2026-03-11 |
| BUG-144 | 消息提醒 | 后端通知权限检测硬编码 `"granted"`，不反映实际 macOS 权限状态；修复：新增 `check_macos_notification_permission()`，通过 `defaults read com.apple.ncprefs apps` 读取 Script Editor（`com.apple.ScriptEditor2`）的通知 flags；flags=0 返回 `"denied"`，否则返回 `"granted"`，未配置时默认 `"granted"`（macOS 新安装默认开启） | 已修复 | 2026-03-11 |
| BUG-143 | 消息提醒 | 系统通知只有声音无弹窗；根因：`notify-rust` 底层用 `NSUserNotification`（macOS 11 起废弃），在 macOS 14/15 上静默返回 Ok 但不展示任何弹窗，导致 osascript fallback 也不触发；修复：彻底移除 `notify-rust` 层，macOS 改为直接用 `osascript display notification`（经用户验证有效，走真实 Notification Center）；移除 `notify-rust` 依赖，零 warning | 已修复 | 2026-03-10 |
| FEAT-C10 | 消息提醒 | 原生系统通知替换旧 `osascript` 方案，接入 `tauri-plugin-notification`；新增 SQLite `notification_history` 持久化通知历史；设置页支持查看历史、未读标记、全部已读、清空历史；Dashboard 铃铛显示未读数；训练/导出/数据集通知统一走原生通知 + 历史记录链路 | 已实现 | 2026-03-10 |
| BUG-142 | 控制台/数据准备 | 消息提醒入口右上角图标改为产品 Logo；修复数据集生成完成提示样本数显示错误 | 已修复 | 2026-03-10 |
| FEAT-C9 | 消息提醒 | 新增消息提醒中心：Sidebar 底部铃铛图标 → 弹窗配置面板；支持 Webhook/Slack/Discord/Telegram Bot/飞书/企业微信/ntfy/Bark/Pushover 共 9 种通道；可按事件（训练完成/失败、数据集生成完成/失败、导出完成/失败）独立开关；每通道支持启用/禁用/编辑/删除/测试；配置持久化到 ~/Courtyard/notification-config.json；HTTP 请求由前端本地发出，无需后端账号；Rust+TS 双端编译零错误；i18n 中英双语 | 已实现 | 2026-03-10 |
| BUG-141 | 设置页 | 设置页新增返回按钮；从项目子页面进入时返回原页面，否则返回上一页 | 已修复 | 2026-03-07 |
| BUG-140 | 测试页 | 右上角设置按钮实际把面板渲染在已折叠的 3.1 区域内，点击后看起来无效；已改为独立显示设置面板，并同步提升测试输入区显著性与默认聚焦 | 已修复 | 2026-03-07 |
| BUG-139 | 训练页/测试页 | 最大文本长度交互不清晰，且测试页多轮对话未传历史消息，易误判为上下文被截断；已改为“预设/自定义”清晰切换，并补齐测试页多轮上下文传递 | 已修复 | 2026-03-07 |
| BUG-138 | 训练页 | 最大文本长度仅支持固定预设，无法输入自定义整数；已补充自定义输入，并确保开始训练/加入队列时提交最新值 | 已修复 | 2026-03-06 |
| BUG-QUANT-FULL | 训练页/训练后端 | 量化模型（4-bit/8-bit）使用 Full 全参数微调时，MLX 在训练中途抛出 `[QuantizedMatmul::vjp] no gradient wrt the quantized weights` 导致训练失败且无明确提示；修复：后端提前拦截并返回清晰错误；前端"Full"方法标签加悬停 Tooltip 说明限制；选中量化模型+Full 时主动弹出警告横幅并提供一键切换 LoRA 按钮；训练日志中出现该错误时智能告警面板给出针对性提示；i18n 中英双语 | 已修复 | 2026-03-04 |
| BUG-D11-VALID-OPTIONAL | 训练页/训练后端 | 导入数据集仅包含 train.jsonl（无 valid.jsonl）时，训练启动被后端拦截为缺少 valid.jsonl，导致 D-11「valid 可选」与训练链路不一致；已修复为训练启动时自动从 train.jsonl 生成 valid.jsonl 兜底文件 | 已修复 | 2026-03-04 |
| FEAT-T3 | 训练页 | 训练页 2.3 新增可折叠「高级选项」区（仅 LoRA/DoRA 显示）：新增「LoRA 缩放策略」选择器，支持「标准（alpha/r）」和「rsLoRA（alpha/√r）」；选中 rsLoRA 时显示适用场景提示；已选 rsLoRA 时折叠标题显示蓝色标签；后端 lora_config.yaml 注入 use_rslora: true；i18n 中英双语 | 已实现 | 2026-03-04 |
| FEAT-D11 | 训练页 | 训练页 2.2 新增「导入数据集」按钮：点击弹出格式说明对话框（文件夹结构/Completion 格式/Chat 格式示例）→ 选择文件夹 → 后端验证 train.jsonl 格式（前5行字段完整性检查）→ 复制到项目 dataset 目录并自动选中；dataset 列表展开详情中"生成方式"显示"外部导入"；i18n 中英双语 | 已实现 | 2026-03-04 |
| FEAT-013 | 在线模型 | 将在线模型列表升级为最新系列：Qwen 切到 3.5(35B/27B/122B)；HF GLM 切到 4.7-Flash-4/8bit；Ollama GLM 保持 4.7-Flash；DeepSeek 置顶加入 R1-0528(8B)；同步更新相关模型链接为最新系列专属页面；修复 llama4 语法 bug | 已实现 | 2026-02-28 |
| FEAT-012 | 训练页 | 2.1/2.2/2.3 节标题、训练进度3指标卡（ETA/健康/趋势）、训练总结4指标卡（时长/TrainLoss/ValLoss/损失改善）全部升级为整区悬停Tooltip；移除2.3旧静态提示文字；i18n双语 | 已实现 | 2026-02-28 |
| FEAT-011 | 导出页 | 4.1/4.2/4.3 节标题Tooltip触发从Info图标升级为整区h3悬停；移除4.1/4.2/4.3展开内副标题；GGUF/MLX/推理服务三个独立卡片的描述文字改为标题悬停Tooltip；i18n双语 | 已实现 | 2026-02-28 |
| FEAT-010 | 数据准备 | 1.1/1.2/1.3 节标题、数据预览Tab、智能分段Tab、已生成数据集标题全部升级为整区悬停Tooltip；移除1.2内联静态提示文字和分段hint静态文字；i18n双语 | 已实现 | 2026-02-28 |
| FEAT-006 | 导出页 | 4.1/4.2/4.3 新增副标题说明+悬停 Tooltip，帮助新用户理解适配器/模型名称/量化含义，i18n 双语 | 已实现（含FEAT-011升级） | 2026-02-28 |
| FEAT-005 | 数据准备 | 智能分段预览「主策略」卡片新增悬停 Tooltip，说明各分段算法适用场景，i18n 双语 | 已实现 | 2026-02-28 |
| FEAT-004 | 数据准备 | 移除 1.1 文件列表底部重复的「智能分段」「合并为单数据集」复选框（已迁移至高级设置） | 已实现 | 2026-02-28 |
| FEAT-003 | 数据准备 | 高级设置 5 个 Tooltip 触发区域从 Info 图标扩大到整个 label 行，悬停任意位置均可触发 | 已实现 | 2026-02-28 |
| FEAT-002 | 全局UI | 新增界面缩放功能；修复 TrainingPage/DataPrepPage/ExportPage 中写死像素字号(text-[10px]/text-[11px])，改为 rem 等比值使其随全局字号缩放 | 已实现 | 2026-02-28 |
| FEAT-001 | 交互体验 | 全局增加 Tooltip 悬浮提示，为 AI 参数等增加详细说明，降低新手门槛 | 已实现 | 2026-02-27 |
| BUG-137 | 设置 | 一键清理缓存按钮无二次确认，直接执行删除，存在误操作风险 | 已修复(新增确认弹窗+i18n双语) | 2026-02-28 |
| BUG-136 | 导出 | Ollama导出成功后主"打开文件夹"打开的是中间工作目录(outputDir)而非实际模型所在的Ollama目录；fused按钮打开的也不是fusedDir本身 | 已修复 | 2026-02-28 |
| BUG-135 | 导出/训练/测试/数据准备 | "打开文件夹"按钮样式统一 | 已修复 | 2026-02-27 |
| BUG-134 | 导出 | 导出成功区主“打开文件夹”与 fused 区“在 Finder 中打开”路径不一致 | 已修复 | 2026-02-27 |
| BUG-133 | 导出 | 导出成功区“打开文件夹”打开路径与界面显示导出目录不一致 | 已修复 | 2026-02-27 |
| BUG-132 | 数据准备 | 删除单个原始文件后，智能分段预览被整体清空 | 已修复(保留剩余文件匹配分段) | 2026-02-27 |
| BUG-131 | 数据准备 | 左侧文件列表点击行空白处无响应，仅点击名称可预览 | 已修复(扩大点击区域并阻止删除冒泡) | 2026-02-26 |
| BUG-130 | 数据准备 | 智能分段右侧区域无需再显示已高亮的当前文件名 | 已修复 | 2026-02-26 |
| BUG-129 | 数据准备 | 英文下分段策略名称过长导致截断显示不全 | 已修复(调整字号/换行) | 2026-02-26 |
| BUG-128 | 数据准备 | 右侧智能分段预览区域，“分段总数”、“平均长度”等数据没有跟随选中文件动态刷新，一直显示总数 | 已修复 | 2026-02-26 |
| BUG-127 | 数据准备/训练/测试/导出 | 所有页面清空按钮直接执行，无确认弹窗，容易误触 | 已修复(统一加确认弹窗) | 2026-02-26 |
| BUG-126 | 数据准备 | 清空后智能分段预览内容（segmentPreview）未清除，仍显示旧数据 | 已修复 | 2026-02-26 |
| BUG-125 | 数据准备 | 清空后退出重进项目，原始文件/数据集全部恢复（清空未删除磁盘数据） | 已修复(新增clear_project_data后端命令) | 2026-02-26 |
| BUG-124 | 数据准备 | 清空只重置内存状态，未调用后端删除命令，磁盘文件未清除 | 已修复 | 2026-02-26 |
| BUG-123 | 数据准备 | 1.2/1.3/高级设置/生成按钮四个模块仍合并在同一卡片，非独立可折叠结构 | 已修复 | 2026-02-26 |
| BUG-122 | 数据准备/训练 | ModelSelector本地模型列表显示全部source(含不可用)，造成认知负担 | 已修复(按mode过滤) | 2026-02-26 |
| BUG-121 | 数据准备 | 1.1完成后1.2不能自动展开到Ollama模型列表；无Ollama时未跳至在线tab | 已修复 | 2026-02-26 |
| BUG-120 | 数据准备 | 1.3生成类型/高级设置/生成按钮被错误归入step2Open折叠块，step2Open=false时全部不可见 | 已修复 | 2026-02-26 |
| BUG-119 | 数据准备 | auto-expand useEffect放置于step2Open声明之前，上传文件后1.2不自动展开 | 已修复 | 2026-02-26 |
| BUG-118 | 数据准备 | 全删按钮为红色线框样式，与确认弹窗红底白字风格不一致 | 已修复 | 2026-02-26 |
| BUG-117 | 数据准备/训练 | ModelSelector源标签颜色固定(Ollama蓝/HF橙)，无法区分可用/不可用状态 | 已修复(动态化) | 2026-02-26 |
| BUG-083 | 存储管理 | 导出过程中的中间融合文件(fused safetensors)在导出完成后未清理，导致每个导出模型额外占用3~5GB空间；中断的训练可能残留空适配器文件夹 | 已修复 | 2026-02-21 |
| BUG-082 | 模型预置 | 在线模型列表包含较多冷门模型（如Mistral等），缺少当前主流高评价模型，且不支持按品牌折叠导致列表过长 | 已修复 | 2026-02-20 |
| BUG-112 | 导出 | 导出到 Ollama 报找不到 `ollama` 命令 | 已修复 | 2026-02-24 |
| BUG-113 | 导出 | Ollama 不支持 `Qwen3ForCausalLM` 架构时直接抛出原始 stderr 日志，用户无法理解 | 已修复 | 2026-02-25 |
| BUG-114 | 导出 | 导出成功卡片顶部“打开文件夹”未优先打开 MLX 融合目录，易误导到 ollamaDir | 已修复 | 2026-02-25 |
| BUG-115 | 导出 | 导出成功结果展示区排版拥挤、字号偏小（text-[10px]等），可读性较差 | 已修复 | 2026-02-25 |
| BUG-116 | 导出 | GGUF 导出遇到不支持的模型架构（如 mistral3）时直接透传原始 Python traceback，用户无法理解是第三方限制 | 已修复 | 2026-02-25 |
| FEAT-E5 | 导出 | 新增「保留 MLX 融合模型」选项（Ollama 导出时勾选）+ 独立「导出为 MLX 模型」功能，输出可直接拖入 LM Studio | 新功能 | 2026-02-25 |
| FEAT-E6 | 导出 | 新增本地推理服务（mlx-lm.server）一键启动/停止，显示 OpenAI 兼容 API 端点，支持 OpenWebUI/Chatbox 等工具接入 | 新功能 | 2026-02-25 |
| FEAT-E7 | 导出 | 新增可折叠「如何使用」连接指南，覆盖 Ollama/MLX/Server/GGUF 四条路径的使用说明 | 新功能 | 2026-02-25 |
| BUG-001 | 项目 | 新建项目失败 `sql.execute not allowed` | 进入项目页→新建→输入名称 | 创建成功并跳转 | 权限错误 | 高 | 已修复 | `capabilities/default.json` 添加 `sql:allow-execute/select`；SQL `$1`→`?`；创建后自动跳转数据准备 |
| BUG-002 | 训练 | 停止训练无效 | 开始训练→点击停止 | 训练停止 | 按钮无效 | 高 | 已修复 | 前端分离 Start/Stop 按钮；后端 PID 注册表 + SIGTERM |
| BUG-003 | 数据准备 | docx/pdf 导入后清洗失败 | 导入 docx/pdf→清洗 | 正确解析 | 乱码 | 中 | 已修复 | 文件对话框+清洗脚本支持 docx/pdf（需 `pip install python-docx PyPDF2`） |
| BUG-004 | 项目 API | Rust `list_projects` 返回空数组 | 调用该命令 | 返回列表 | 空数组 | 低 | 不影响功能 | 前端直接查 SQLite，不走该 IPC |
| BUG-005 | 项目目录 | 目录以 UUID 命名 | 新建项目→查看目录 | 含项目名 | UUID | 低 | 设计决策 | UUID 避免命名冲突/特殊字符 |
| BUG-006 | 导出 | Ollama 未安装时可导出 | 未装 Ollama→导出页 | 禁用导出 | 已修复 | 低 | 已修复 | 页面加载检测 Ollama，未安装显示警告并禁用按钮 |
| BUG-007 | 项目 | 删除顺序导致数据不一致 | 删除项目→目录失败 | DB 保留 | 已修复 | 低 | 已修复 | 先删目录再删 DB |
| BUG-008 | 数据准备 | 生成问答对：硬编码模型+无进度+依赖链路不透明 | 清洗后→生成 | 可选AI/内置生成，有进度条 | 已重构 | 中 | 已修复(重构) | fix3: 多源生成(Ollama/内置规则)；Ollama模型自动检测下拉；ModelScope下载引导；手动添加模型路径；实时进度条；中文i18n模式标签 |
| BUG-009 | 交互体验 | 数据准备到训练流程缺少引导 | 导入→清洗→生成 | 指引下一步 | 仅展示文件列表 | 中 | 已修复 | 添加 4 步流程指示器（导入→清洗→生成→训练）；数据集就绪后显示「去训练」跳转按钮 |
| BUG-010 | 数据准备 | AI生成数据集全部失败(0成功) | 选AI生成→glm-4.7-flash→开始 | 成功生成问答对 | 全部JSON解析失败 | 高 | 已修复 | 根因: thinking模型content为空(内容在thinking字段)+num_predict=512不够; 修复: think:false禁用思考+2048token+双字段提取+/api/chat接口 |
| BUG-011 | 数据准备 | AI生成无反馈+无法停止+无法续传 | 开始AI生成 | 可见AI活动日志，可停止，可续传 | 仅显示进度条 | 中 | 已修复 | 添加AI实时日志面板(右侧)；停止按钮(SIGTERM)；增量保存(每条即写)；断点续传(--resume) |
| BUG-012 | 训练 | 基础模型只能手动输入，无法选择本地模型 | 进入训练页 | 下拉选择Ollama模型 | 只有文本输入框 | 中 | 已修复 | 训练页模型字段改为Ollama模型下拉+刷新按钮 |
| BUG-013 | 控制台 | Python就绪但横幅显示"环境未配置" | Python✅+mlx-lm✖→控制台 | 显示"mlx-lm未安装" | 显示"Python环境未配置" | 中 | 已修复 | 拆分为两个独立横幅：Python未配置(黄色) / mlx-lm未安装(蓝色) |
| BUG-014 | 设置 | mlx-lm安装按钮显示成功但实际未检测到 | 点击安装→显示成功→mlx-lm仍"未安装" | mlx-lm状态变为已安装 | 状态不变 | 高 | 已修复 | 根因: 检测依赖setup_env.py但该文件未打包进bundle; 修复: 改为python -c内联检测 + tauri.conf.json添加scripts resources |
| BUG-015 | 数据准备 | 切换页面后生成进度/日志/停止按钮丢失 | 生成中→切换到训练页→切回 | 状态保持 | 状态全部重置 | 高 | 已修复 | 创建zustand全局generationStore，事件监听器只初始化一次不随组件卸载 |
| BUG-016 | 系统 | 合盖/休眠后AI生成可能超时失败 | 生成中→合盖→打开 | 继续运行 | 网络超时 | 中 | 已修复 | 用caffeinate -i包裹生成/清洗任务阻止系统空闲睡眠 |
| BUG-017 | 训练 | 训练直接出错失败 | 选模型→开始训练 | 开始训练 | 报deprecated错误 | 高 | 已修复 | 根因: `python -m mlx_lm.lora`已弃用; 修复: 改为`python -m mlx_lm lora --train`+添加`--train`标志+caffeinate包裹+同时捕获stdout和stderr+验证数据集存在 |
| BUG-018 | 训练 | 训练无进度可视化 | 训练中 | 能看到loss曲线和进度 | 只有日志文本 | 中 | 已修复 | 添加SVG Loss曲线图(Train+Val双线)+迭代进度条(Iter N/Total)+实时loss数值显示；解析mlx-lm输出的Iter/Train loss/Val loss |
| BUG-019 | 数据准备 | 中断生成后留下冗余未完成文件 | 生成中→停止 | 清理未完成文件 | 留下部分train.jsonl | 中 | 已修复 | 生成前备份已有数据集(.bak)；成功则删备份；停止/失败则恢复备份、删除部分文件；移除"继续生成"按钮 |
| BUG-020 | 数据准备 | 无法查看本地数据存储位置 | 数据准备/训练页 | 有打开文件夹按钮 | 无导航 | 低 | 已修复 | 添加open_project_folder命令(Rust)；数据准备页和训练页均添加"打开文件夹"按钮，在Finder中打开项目目录 |
| BUG-021 | 数据准备 | 清洗和生成是分离的串行步骤，用户困惑 | 需先点清洗再点生成 | 一键完成 | 两个独立按钮 | 中 | 已修复 | 合并为pipeline：用户点"生成"→系统自动检测是否需清洗→阶段1清洗→阶段2生成；步骤指示器简化为3步(导入→生成数据集→训练)；分阶段进度显示 |

| BUG-022 | 训练 | 基础模型只有文本输入+Ollama下拉，缺少本地模型扫描和在线模型 | 训练页→选模型 | 多来源统一选择 | 仅Ollama下拉 | 中 | 已修复 | fix7~fix12: 统一ModelSelector组件，支持Ollama/本地HF/ModelScope/在线模型，分类radio+折叠面板+灰显不可用+已下载标识 |
| BUG-023 | 训练 | 训练状态页面切换后丢失 | 训练中→切页→切回 | 状态保持 | 状态重置 | 中 | 已修复 | fix7: 训练状态持久化到Zustand store |
| BUG-024 | 测试 | 测试页面无法选择已训练适配器 | 测试页 | 下拉选择adapter | 无列表 | 中 | 已修复 | fix7: 测试页重构，适配器下拉+分类显示 |
| BUG-025 | 导出 | 导出页适配器选择和基础模型联动缺失 | 导出页 | 选适配器自动带入基础模型 | 需手动输入 | 中 | 已修复 | fix16: 收起式下拉适配器+移除基础模型手动输入+自动带入 |
| BUG-026 | 导航 | 缺少项目→子页面的层级导航 | 选项目后 | 清晰的导航层级 | 扁平导航 | 中 | 已修复 | fix15: 导航重构为一级(控制台+项目)/二级(数据准备+训练+测试+导出)，自动加载最近项目 |
| BUG-027 | 项目 | 删除项目无确认弹窗 | 项目列表→删除 | 确认后删除 | 直接删除 | 中 | 已修复 | fix17: 删除确认弹窗，显示项目名称 |
| BUG-028 | 项目 | 不支持项目重命名 | 项目列表 | 可重命名 | 无此功能 | 低 | 已修复 | fix17: 行内编辑重命名，Enter确认/Esc取消 |
| BUG-029 | 项目 | 空项目时新建按钮在右上角不易发现 | 首次进入项目页 | 居中大按钮 | 右上角小按钮 | 低 | 已修复 | fix17: 无项目时居中显示大图标+大按钮，有项目时保持右上角 |
| BUG-030 | 全局 | 四个工作页面缺少统一进度指引 | 数据准备/训练/测试/导出 | 统一进度条 | 各页面独立 | 中 | 已修复 | fix17: 统一StepProgress组件(进度条+子步骤时间轴)，当前栏目放大高亮，完成子步骤绿色标记 |
| BUG-031 | 全局 | 页面标题无序号，用户不清楚流程顺序 | 各工作页面 | 有序号标题 | 无序号 | 低 | 已修复 | fix17: 所有页面标题加序号(1.数据准备/2.训练模型/3.测试模型/4.导出模型)，子标题加二级序号(1.1~4.3) |
| BUG-032 | 数据准备 | 上传按钮在右上角，离操作区域远 | 数据准备页 | 按钮在区域内 | 右上角 | 低 | 已修复 | fix17: 上传按钮移入1.1区域内，空文件时居中显示 |
| BUG-033 | 数据准备 | 生成类型选项卡缺少标题 | 数据准备→生成 | 有标题 | 直接显示tab | 低 | 已修复 | fix17: 新增"1.4 生成类型"标题 |
| BUG-034 | 模型选择 | 在线模型分类交互与来源策略不一致 | 训练/数据准备→在线模型 | 按发布时间排序、默认收折、来源清晰 | 已修复 | 中 | 已修复 | fix20: 在线模型按发布时间排序；默认全部收折；新增Mistral/Phi；数据准备仅Ollama来源并移除“调整模型来源”；训练仍为HF/镜像/魔塔 |
| BUG-035 | 设置 | 训练页跳转设置后未准确定位“模型下载源” | 训练→调整模型来源 | 设置页定位到下载源模块（居中） | 已修复 | 低 | 已修复 | fix20: 跳转增加`focus=download-source`，设置页解析参数后scrollIntoView居中定位 |
| BUG-036 | 模型选择 | 在线模型描述含语言倾向（如"中文能力强"），误导非中文用户 | 在线模型→查看Qwen 3 4B描述 | 客观中立的能力描述 | 显示"中文能力强" | 中 | 已修复 | fix21: chineseGood→versatile("多功能全能型"/"Versatile all-rounder")；移除chineseBetter/google/geminiFamily等不再使用的i18n键 |
| BUG-037 | 模型选择 | 在线模型品牌分类只有点击名称文字才能展开 | 在线模型→点击分类行空白区域 | 整行可点击展开 | 无反应 | 中 | 已修复 | fix21: 品牌行改为整行div onClick触发toggle；"更多"按钮stopPropagation防冲突 |
| BUG-038 | 全局 | Python脚本日志输出硬编码中文，英文UI下仍显示中文日志 | 切换英文→数据准备→开始生成 | 日志按UI语言显示 | 所有日志为中文 | 中 | 已修复 | fix22: 新建Python i18n模块(scripts/i18n.py+locales/en.json+zh-CN.json)；6个Python脚本全部改用t()翻译键；Rust命令新增lang参数透传；前端invoke调用传递i18n.language；AI系统提示词也按语言切换 |
| BUG-039 | 训练 | 数据集样本数<batch_size时训练崩溃(ValueError) | 生成少量数据(如valid仅3条)→开始训练(batch_size=4) | 自动适配batch_size | ValueError: Dataset must have at least batch_size=4 examples but only has 3 | 高 | 已修复 | fix23: 训练启动前统计train.jsonl和valid.jsonl行数，自动将batch_size钳制到min(batch_size, min(train_count, valid_count)) |
| BUG-040 | 训练 | ETA 完成态显示异常 | 训练迭代达到总数后观察 ETA 卡片 | 显示“已完成” | 显示“正在估算中...” | 低 | 已修复 | fix24: ETA 文案判断改为优先检查 remainingIters<=0，避免 eta=0 被误判为未估算 |
| BUG-041 | 数据链路 | 训练数据与上传文件不一致 | 导入英文文本→数据准备→训练 | 使用当前上传数据 | 出现历史中文内容 | 高 | 已修复 | fix26: 生成前强制重清洗；训练必须显式选择数据集版本；移除后端dataset默认回退；Ollama生成增加语言一致性约束与脚本校验 |
| BUG-042 | 数据准备 | 开始生成后直接失败 `Generation exited with code 2` | 点击"开始生成" | 正常进入生成 | `--mode ''`空字符串导致argparse失败 | 高 | 已修复 | fix28: 根因是stale closure——`startGenerationStep`被useEffect([])闭包捕获时genMode为初始值空串；修复：从`useGenerationStore.getState()`读取最新表单值；fix27已做的stderr透传+--lang降级保留 |
| BUG-043 | 数据准备 | 点击"开始生成"后看不到顶部预览/日志区 | 页面滚动到中下部后点击"开始生成" | 自动回到顶部看到预览区 | 需手动滚动 | 中 | 已修复 | fix27: 点击开始后自动滚动到页面顶部并定位预览面板 |
| BUG-044 | 数据准备 | 模式适配检测仅基于文件扩展名硬编码，不准确 | 导入txt文件→查看模式检测 | 基于内容智能推荐 | 纯扩展名判断 | 中 | 已修复 | fix28: 新增后端`sample_raw_files`命令读取文件前2000字符；前端`analyzeContentForModes`综合分析扩展名+内容特征(散文/对话/标题/结构化)；检测结果以图标(Star/Check/AlertCircle)显示在tab右上角 |
| BUG-045 | 数据准备 | 完成1.1+1.2后1.3默认选第一个而非推荐模式 | 导入文件→选方式→观察默认 | 智能选推荐模式 | 始终选"知识问答" | 低 | 已修复 | fix28: 文件加载后自动采样→分析→按优先顺序(qa>instruct>style>chat)选中第一个"推荐"模式 |
| BUG-046 | 训练 | 停止训练后 Loss 空态文案错误 | 开始训练后尽快停止（曲线点数不足） | 显示“训练已停止”文案 | 显示“等待训练数据...” | 低 | 已修复 | fix29: Loss 空态文案按状态区分（running=waiting，stop=lossStopped，其它=lossNoData） |
| BUG-047 | 训练 | 训练完成后页面未自动回顶部 | 在日志/预览区域等待训练完成 | 自动滚动到顶部先看训练总结 | 仍停留在中下部区域 | 中 | 已修复 | fix29: 仅在 running→completed 转场触发回顶；优先滚动主容器 `#app-main-scroll`，失败回退 `window.scrollTo` |
| BUG-048 | 训练 | 训练总结中保存 Markdown/CSV/SVG/PNG 按钮无效 | 训练完成→点击保存按钮 | 功能可用或不展示 | 点击无反应 | 中 | 已修复 | fix29: 移除4个无效按钮及对应导出处理代码，仅保留“复制报告 Markdown”和“系统分享” |
| BUG-049 | 测试 | A/B 模块干扰主测试流程 | 打开测试页观察首屏 | 先看到聊天测试 | 首屏被 A/B 占据 | 中 | 已修复 | fix30: A/B 改为“高级A/B对比”折叠入口，默认收起 |
| BUG-050 | 测试 | 输入框默认高度过小 | 打开测试页输入区 | 多行输入舒适 | 默认仅1行偏紧凑 | 低 | 已修复 | fix30: 输入框改为默认3行并设置最小高度 |
| BUG-051 | 测试 | 适配器区默认展开占用空间 | 打开测试页 | 聊天区优先可见 | 3.1默认展开 | 低 | 已修复 | fix30: 适配器选择区默认收起 |
| BUG-052 | 测试 | 单页仍显示小节时间轴 | 打开测试页 | 简洁聚焦聊天 | 显示时间轴占空间 | 低 | 已修复 | fix30: 移除测试页 StepProgress |
| BUG-053 | 测试 | A/B 模式文案不易理解 | 展开A/B查看模式 | 一眼理解模式差异 | 自动/顺序/并行含义不清 | 中 | 已修复 | fix30: 新增模式解释文案（自动/顺序/并行） |
| BUG-054 | 数据准备 | 智能分段预览读取历史清洗结果，与当前新增 raw 文件不一致 | 导入新测试文件（如 `markdown_case.md`）→不启动新清洗直接观察 1.3 智能分段预览 | 预览仅显示当前 raw 文件对应结果；未重清洗时应为空提示 | 仍显示历史项目内容（旧 `segments.jsonl`） | 高 | 已修复 | fix31: `preview_clean_segments` 按当前 raw 文件过滤 `source_file`，并校验 `segments_manifest.json` 与 raw 清单一致；`clean_data.py` 新增 manifest 输出 |
| BUG-055 | 数据准备 | Ollama 生成在单段样本下出现 0 成功并报 `Generation exited with code 1` | 导入 `markdown_case.md` → AI 生成（qa） | 至少生成可用训练样本 | JSON 解析/字段兼容导致 1 段全失败 | 高 | 已修复 | fix32: 增加中英键名归一化、宽松键值提取回退；小样本语言不一致时保留样本 |
| BUG-056 | 数据准备 | AI 生成日志语言不随 UI 语言切换 | 切换 UI 为英文→开始 AI 生成 | 日志显示英文 | 日志仍为中文 | 中 | 已修复 | fix33: 使用全局 `i18nGlobal.language` 替代闭包中的 `i18n.language` |
| BUG-057 | 数据准备 | 清洗/生成使用历史 cleaned 数据导致内容不一致 | 导入新文件→直接开始生成 | 仅使用当前 raw 文件 | 可能混入旧 segments.jsonl | 高 | 已修复 | fix33: `start_cleaning` 执行前先删除整个 `cleaned/` 目录再重建 |
| BUG-058 | 数据准备 | 生成完成后用户需手动滚动才能看到 1.4 已生成数据集 | 完成 AI 生成 | 自动滚动到 1.4 区域 | 停留在日志区 | 中 | 已修复 | fix33: generationStore `dataset:complete` 后调用 `_scrollToDatasets` 回调 |
| BUG-059 | 数据准备 | 1.4 数据集列表无法查看原始文件名、生成类型、生成方式 | 查看已生成数据集列表 | 可展开查看详情 | 仅显示时间/数量/大小 | 中 | 已修复 | fix33: Rust 写入 `meta.json`，前端点击可展开详情行 |
| BUG-060 | 数据准备 | 无法快速区分最新生成的数据集 | 生成完成后查看列表 | 最新项有明显标识 | 所有项样式一致 | 低 | 已修复 | fix33: 监听 `dataset:version` 记录 `newVersionIds`，显示 Sparkles + NEW 徽章 |
| BUG-061 | 数据准备 | 数据集列表无分页，大量版本时不易浏览 | 生成超过 10 个版本 | 默认 10 条并支持翻页 | 全部平铺 | 低 | 已修复 | fix33: 默认每页 10 条，超出显示翻页按钮及页码 |
| BUG-062 | 数据准备 | 右侧面板标题不一致且边框未与左侧对齐 | 查看数据准备页右侧面板 | 标题为"数据预览"且外框与左侧 1.1 风格一致 | 标题"AI 生成日志"且无外卡片框 | 低 | 已修复 | fix33: i18n 改为"数据预览"；右侧增加卡片外框+内容区内边框 |
| BUG-063 | 数据准备 | chat 模式 plain_case.txt 生成失败 `Generation exited with code 1` | 导入 plain_case.txt → 选多轮对话 → AI 生成 | 至少生成可用训练样本 | JSON 截断导致解析到内部 `{role,content}` 对象，0 成功 | 高 | 已修复 | fix34: chat 模式 `num_predict` 从 2048→4096；`extract_key_value_fallback` 增加截断 JSON 回收：收集所有 `{role,content}` 对象组成 conversations |
| BUG-064 | 数据准备 | 生成失败后 1.4 数据集列表变空，历史数据集消失 | 生成失败（code 1）后查看 1.4 | 历史数据集仍正常显示 | 列表显示 0 条 | 高 | 已修复 | fix34: `dataset:error` 事件处理器增加 `_reloadFiles()` 调用，恢复历史数据集列表 |
| BUG-065 | 数据准备 | 1.4 列表行内显示 train/valid 不易理解且占空间 | 查看数据集列表 | 折叠行简洁，详细信息在展开中 | train:1 · valid:1 在折叠行内 | 低 | 已修复 | fix34: 移除折叠行 train/valid，移入展开详情 |
| BUG-066 | 数据准备 | train/valid 专业术语普通用户不易理解 | 查看数据集详情 | 有中英文描述说明用途 | 仅显示 train/valid | 低 | 已修复 | fix34: i18n 增加"训练集 (train)"/"验证集 (valid)" 描述，展开详情中显示 |
| BUG-067 | 数据准备 | NEW 徽章含多余图标且不跟随主题 | 生成后查看最新数据集 | 纯文字 NEW，跟随主题色 | 含 Sparkles 图标，使用 primary 色 | 低 | 已修复 | fix34: 移除 Sparkles 图标，改用 `bg-success/15 text-success` 跟随主题 |
| BUG-068 | 数据准备 | AI 生成期间 1.4 数据集列表被清空，已有数据集消失 | 开始 AI 生成→查看 1.4 | 已有数据集始终可见 | 列表显示 0 条"暂无数据集" | 中 | 已修复 | fix34: 移除 `startGenerationStep` 中的 `setDatasetVersions([])`，生成期间保留历史列表 |
| BUG-069 | 导出/模型识别 | Ollama 导出路径、打开文件夹路径、本地模型识别列表不一致 | 配置自定义 `model_paths.ollama`，并在 daemon 未带 `OLLAMA_MODELS` 启动后执行导出 | 导出结果路径展示、打开目录、模型列表三方一致，模型命名一致可识别 | 导出后模型可能落在 `~/.ollama/models`，但界面按配置路径显示，列表缺失新模型 | 高 | 已修复 | fix35: 新增 daemon-aware `resolve_ollama_models_dir`，统一 export/list/scan/open cache；Export 成功区优先展示/打开 `manifest_dir`；ModelSelector 监听 `export:complete` 自动刷新 |
| BUG-070 | 数据准备/训练 | 模型选择器本地模型列表中出现"已训练适配器"分组，该场景下无法使用适配器 | 进入数据准备/训练模型选择→本地已有模型 | 仅显示可用的 Ollama/HuggingFace/ModelScope 模型 | 显示"已训练适配器"分组（标记不可用），造成认知负担 | 中 | 已修复 | fix36: ModelSelector 仅在 mode==="export" 时将适配器加入 combinedModels，且仅在 export 模式才调用 loadAdapters |
| BUG-071 | 导出 | 导出 4.1 适配器列表无删除功能，用户无法在应用内管理磁盘空间 | 导出页→4.1 选择适配器展开列表 | 每个适配器可直接删除（单击变确认、再次点击执行） | 仅能选择，无删除入口 | 中 | 已修复 | fix36: 后端新增 `delete_adapter` 命令（含 /adapters/ 路径安全校验）；前端适配器行嵌入删除按钮（两步确认防误删） |
| BUG-072 | 导出 | 导出 4.1「打开文件夹」进入单个 UUID 适配器内部，无法看到所有适配器 | 导出页→4.1→点击打开文件夹 | 打开项目的 adapters/ 目录（所有适配器列表） | 进入单个适配器 UUID 文件夹内（显示 safetensors 等文件） | 低 | 已修复 | fix36: 有选中适配器时截取 parent 路径（adapters/）打开，无选中时打开项目根目录 |
| BUG-073 | 导出 | 确认删除按钮显示硬编码中文"确认删除"，未走 i18n | 英文界面→导出 4.1→点击删除→点确认 | 显示"Confirm Delete" | 显示"确认删除"（硬编码） | 低 | 已修复 | fix37: en/zh-CN export.json 新增 confirmDelete/deleteAdapter/noWeights/deleteAll 键；ExportPage 改用 t() |
| BUG-074 | 数据准备/训练 | 本地模型列表展开来源分组时，页面无视觉变化（内容在滚动区外，用户不知道） | 模型选择→本地→点击底部 HuggingFace 展开 | 列表自动滚动使被展开的分组头部可见 | 仅产生滚动条，容器不自动滚动 | 中 | 已修复 | fix37: ModelSelector 新增 sourceHeaderRefs，toggleSource 展开时用双 requestAnimationFrame 计算相对偏移并 scrollTo({behavior:"smooth"}) |
| BUG-075 | 导出 | 4.1 适配器列表只显示有权重的适配器，历史失败训练的残留文件夹不可见、无法在应用内删除 | 导出页→4.1 打开适配器列表 | 显示全部适配器，无权重的标注"训练未完成"且只能删除 | 仅显示 has_weights=true 的适配器，其余不可见 | 中 | 已修复 | fix37: loadAdapters 改为加载全部适配器；无权重适配器显示 noWeights 徽章且禁止选择；新增"删除全部适配器"选项 |
| BUG-076 | 本地模型/Ollama路径 | 本地模型未区分默认与自定义 Ollama 路径 | 设置自定义 `model_paths.ollama` 后刷新本地模型 | 按「Ollama / 自定义 Ollama」分组展示，并可分别打开目录 | 已修复 | 中 | 已修复 | fix38: `scan_local_models` 同时扫描默认+自定义路径；新增 `ollama_custom` 来源；`open_model_cache` 支持 `ollama_custom` |
| BUG-077 | 设置 | 存储页导出默认路径展示硬编码，且缺少 Ollama 路径有效性提示 | 打开设置→存储 | 默认导出路径使用后端实际路径模板；显示 default/effective 与有效性提示 | 已修复 | 中 | 已修复 | fix38: `get_app_config` 新增 `default_export_root`；接入 `get_ollama_path_info`；新增路径有效性提示文案 |
| BUG-078 | 导出 | 导出成功区路径信息不完整，无法区分默认/自定义 Ollama 路径 | 导出完成后查看成功卡片 | 同时显示 output/ollama/manifest 路径并标注默认/自定义 | 已修复 | 中 | 已修复 | fix38: ExportPage 新增路径明细分块展示 + 默认/自定义标识；移除 manifest 硬编码 fallback |
| BUG-079 | 导出 | 偶发“Step is still running”状态卡住 | 首次进入导出页后立即导出或快速切页 | 导出完成/失败后状态正确结束 | 已修复 | 高 | 已修复 | fix38: `exportStore`/`exportGgufStore` 监听初始化 Promise 化，并在导出前 `await initListeners()` 避免漏事件 |
| BUG-080 | 导出 | 导出后验证失败（`unable to load model`） | 导出后执行验证或 `ollama run` | 可正常加载并通过验证 | 已修复 | 高 | 已修复 | fix39: 清理复用 `fused/` 残留文件；优先单文件 safetensors 并去重张量；导出后增加运行态验证 |
| BUG-081 | 数据准备 | 本地模型可选但 daemon 运行态不可用 | 配置自定义路径并进入数据准备模型选择 | 仅可选择 daemon 实际可见模型 | 已修复 | 中 | 已修复 | fix39: dataprep 可用性改为以 `list_ollama_models` 返回为准，不可用项显示禁用原因 |
| BUG-082 | 数据准备/模型选择 | fix40 假将文件系统扫描加入 `list_ollama_models`，导致自定义路径模型看似可选但实际调用时仍 404 | 配置自定义路径，选择该路径下模型并执行生成 | 选择后能正常调用 | 已回滚 | 高 | 已修复 | fix41: 回滚 fix40，恢复 fix39 daemon-only 行为，转而增加 404 專属错误引导 banner 和自动修复功能 |
| BUG-083 | 数据准备 | 选择了 daemon 不可识模型后生成失败，仅显示红色错误文本，用户不知如何修复 | 选择 daemon 不可见的 Ollama 模型并运行生成 | 显示路径配置引导信息和命令示例 | 已修复 | 高 | 已修复 | fix41: Python 检测到 HTTP 404 时 emit is_path_mismatch=true；前端显示黄色引导 banner，含示例 launchctl 命令 + 自动修复按钮（launchctl setenv + 重启 Ollama） |
| BUG-084 | 导出/路径语义 | `export_path`（导出存储位置）与 Ollama 导出路径语义混淆，导致行为与文案不一致 | 设置"导出存储位置"→执行 Ollama 导出 | 路径语义明确：Ollama 导出走 `model_paths.ollama`，`export_path` 仅用于 GGUF | 曾出现语义冲突（fix44 临时接入 `export_path`） | 中 | 已修复 | 最终以 fix45 规则为准：回退 fix44，Ollama 导出使用 `model_paths.ollama`；设置页文案改为"GGUF 导出目录"，避免混淆 |
| BUG-085 | 设置/Ollama路径 | 设置页点击“恢复默认”后 `effective_path` 仍显示旧自定义路径，模型下拉仍读旧路径 | 设置自定义路径→恢复默认→查看生效路径与模型列表 | 清理 `launchctl` 环境并重启 Ollama，生效路径回到默认 | 已修复 | 高 | 已修复 | fix43: 根因是旧 `ollama serve` 进程未及时退出；`restart_ollama_app` 增加主动 `pkill` + 等待旧进程退出（最多 4s）+ 轮询等待新 daemon 出现（最多 6s），确保 `resolve_ollama_models_dir` 读到新 daemon 路径 |
| BUG-086 | 设置/Ollama路径 | 恢复默认后 Ollama 重启仍用外置硬盘路径，无任何提示说明原因 | 设置自定义路径→恢复默认→Ollama 重启后查看生效路径 | 提示用户存在系统级持久化 OLLAMA_MODELS 设置并给出清除命令 | 恢复默认静默无提示，用户不知道根因 | 中 | 已修复 | fix45: `resetPath` 在 effective_path ≠ default_path 时显示 `ollamaEffectivePathPersistHint`，解释系统级 launchctl/shell 变量覆盖问题并给出 `launchctl unsetenv OLLAMA_MODELS` 命令 |
| BUG-087 | 导出/路径 | "导出存储位置"标签描述为"Ollama 模型导出目录"，但实际只用于 GGUF 导出，导致用户混淆 | 查看设置页导出存储位置描述 | 描述准确说明该路径仅用于 GGUF 导出 | 描述写"Ollama 模型导出的输出目录"，与实际不符 | 中 | 已修复 | fix45: i18n 将 `exportPath` 改为"GGUF 导出目录"，`exportPathDesc` 明确说明不影响 Ollama 导出；`resolve_ollama_models_dir_for_export` 回退为只读 `model_paths.ollama` |
| BUG-088 | 导出/"打开文件夹" | 成功卡片"打开文件夹"打开的是中间文件目录而非 Ollama 模型目录 | 导出成功→点击"打开文件夹" | 打开 OLLAMA 模型目录（含 blobs/manifests，便于复制或删除） | 打开项目临时输出目录 `export/ollama/fused` | 中 | 已修复 | fix45: 按钮改为优先打开 `ollamaDir`（Ollama 模型根目录），其次 `manifestDir`，最后 `outputDir` |
| BUG-089 | 导出/互锁 | Ollama 导出进行中时 GGUF 导出按钮仍可点击，反之亦然 | Ollama 导出中→尝试点击"导出为 GGUF" | 对方按钮禁用，禁止并发导出 | 两个导出按钮互不影响，可同时点击 | 中 | 已修复 | fix45: GGUF 按钮增加 `disabled={isExporting || ...}`；Ollama 按钮增加 `disabled={isGgufExporting || ...}` |
| BUG-090 | 模型选择/Ollama路径 | 模型列表显示"自定义 Ollama"和"Ollama"两个分组，让用户误认为有两套 Ollama | 设置自定义 Ollama 路径后查看模型选择器本地模型 | 只显示一个"Ollama"分组，内容来自当前 daemon 实际生效路径 | 显示两个分组（"Ollama"=默认路径、"自定义 Ollama"=配置路径）| 中 | 已修复 | fix46: `scan_local_models` 删除双路径扫描，改为 `resolve_ollama_models_dir()`（daemon 感知）扫描单一生效路径；`open_model_cache` 同步修改；移除前端 `ollama_custom` source label 和颜色定义；i18n 移除 `sourceLabels.ollama_custom` |
| BUG-091 | 代码清理/隐私 | 清理冗余并移除路径示例中的个人路径样式 | 代码走查与全文检索 `/Users`、`/Volumes` | 关键业务代码无冗余死代码；注释与文案不出现个人绝对路径 | 已修复 | 中 | 已修复 | fix47: 删除未使用导入与未使用函数；`dataPrep` i18n、`download_model.py`、`USAGE.md` 路径示例统一改为 `/path/to/...`；验证 TS/Rust 编译通过 |
| BUG-092 | 数据准备 | 停止生成按钮无效 | 生成中→点击"停止生成" | 生成立即终止 | 按钮无响应，进程继续运行 | 高 | 已修复 | `generate_dataset` 中 `GENERATION_PID.store(0)` 在 `child.wait()` 之前执行，导致 `stop_generation` 读到 0 无法 kill；修复：PID 清零移至 `child.wait().await` 之后 |
| BUG-093 | 数据准备 | 全部清除无确认弹窗，第二次点击直接删除 | 点击垃圾桶图标 | 弹出确认对话框 | 两次点击直接清除，无弹窗 | 中 | 已修复 | 移除双击确认状态机，改为标准 Modal 弹窗（`showClearAllDialog`），含取消/确认按钮 |
| BUG-094 | 数据准备 | 停止生成后前端状态卡住（进度条不消失） | 停止生成→后端 stopped 事件未触发 | 界面恢复空闲状态 | 清洗/生成状态仍为 true，按钮不可用 | 中 | 已修复 | `handleStop` 增加前端兜底重置：无论后端事件是否触发，均同步清除 `cleaning`/`generating`/`taskLock` |
| BUG-095 | 数据准备 | 1.2 步骤描述文本位置不合理且分行 | 进入数据准备 1.2 | 描述与顶部介绍合并，随模式动态切换 | 描述文本在红色显眼位置，独立分行 | 低 | 已修复 | 将 AI 生成和内置规则的描述合并至 1.2 顶部的 `generate.hint` 区域，移除下方独立段落 |
| BUG-096 | 数据准备 | AI 生成模式选项默认收起，且默认选在线模型 | 进入数据准备 1.2 选择 AI 生成 | 默认展开本地模型选项，无本地模型时展开在线模型 | 需点击展开，默认不确定 | 低 | 已修复 | `ModelSelector` 增加 `defaultOpen` 属性；本地模型 `usableModels === 0` 时自动切换至在线模型 tab |
| BUG-097 | 模型选择 | 在线模型包含不可本地运行的云端模型，且 Ollama 缺少特定模型 | 检查在线模型列表 Qwen 品牌 | 仅展示可本地运行的模型，并保证 Ollama 与 HF 列表逻辑对齐 | 包含 `Qwen3.5-397B-A17B-4bit` (250GB)；Ollama 缺少 Qwen 3.5 | 中 | 已修复 | 经核实，Ollama 与 HuggingFace 社区目前缺乏适合本地单机运行的轻量级 Qwen 3.5 模型（可用版本多为云端或超大参数 MoE）。为保持列表纯粹和双端对齐，已将 Qwen 3.5 相关模型从 Ollama 和 HF 在线推荐列表中统一移除，仅保留成熟可用的 Qwen 3 系列 |
| BUG-098 | 模型选择 | 最新模型未排在最前面 | 查看模型选择下拉列表 | 最新发布的模型自动排在最前面 | 默认排序未严格按时间倒序 | 低 | 已修复 | `ModelSelector` 内部已通过 `sortOnlineGroupsByRelease` 函数确保所有分组内的版本严格按 `releasedAt` 时间戳倒序排列 |
| BUG-099 | 模型选择 | 在线模型列表存在多项严重问题：cloud-only 模型、ID 错误、尺寸严重失实、量化格式未标注 | 逐一访问 HuggingFace 官方页面及 Ollama library 核查每个在线模型 | 所有模型 ID 真实可用、量化格式清晰标注、尺寸标注准确 | (1) Ollama `glm-5:latest`/`kimi-k2.5:latest` 仅有 cloud tag，不能本地运行；(2) MLX Kimi 品牌（K2.5/K2-Thinking/K2-Instruct-4bit）实际 1.24 TB，标注 ~16GB；(3) HF `mlx-community/GLM-5-4bit` 基于 355B MoE 模型，4-bit ≈ 177GB，标注 ~24GB 严重失实；(4) `DeepSeek-R1-Distill-Llama-8B-4bit-mlx` ID 末尾多余 `-mlx`，HuggingFace 实际路径为 `DeepSeek-R1-Distill-Llama-8B-4bit`；(5) 所有 HF 模型 label 未标注量化格式（4-bit/8-bit/MXFP4） | 高 | 已修复 | 移除 Ollama GLM-5、Kimi 品牌；移除 HF GLM-5-4bit；移除 HF/Ollama Kimi 品牌；修正 DeepSeek Llama 8B ID；为所有 HF 模型 label 补充量化格式标注（如"Qwen 3 14B · 4-bit DWQ"、"Ministral 3 14B · MXFP4"等） |
| BUG-100 | 设置/存储 | 训练、数据集生成或导出进行中时，“一键清理缓存”按钮仍可点击，存在并发清理风险 | 已修复 | 2026-02-21 |
| BUG-101 | 首页/存储 | 控制台首页缺少缓存容量展示与一键清理入口，且关于模块应用版本号未与打包版本同步（硬编码为 0.1.0 MVP） | 已修复 | 2026-02-21 |
| BUG-102 | 数据准备/重试 | 失败样本批量重试在未重新清洗场景会被错误拦截 | 已修复 | 2026-02-22 |
| BUG-103 | 数据准备/脚本兼容 | 质量评分/重试参数传入后，内置与 legacy 脚本可能 code 2 退出 | 已修复 | 2026-02-22 |
| BUG-104 | 数据准备/项目隔离 | 停止数据准备后切换项目仍残留选择项、预览与日志 | 已修复 | 2026-02-22 |
| BUG-105 | 训练/数据准备 | 停止数据准备或训练后，预览/日志区域未即时清空 | 已修复 | 2026-02-22 |
| BUG-106 | 全模块/项目隔离 | fix50 不彻底：DataPrep 1.1文件列表/智能分段/TrainingPage/TestingPage/ExportPage 切换项目仍出现跨项目数据残留 | 已修复 | 2026-02-23 |
| BUG-107 | 数据准备/智能分段 | fix51 key 方案未生效（Route element 上的 key 不被 Outlet 传递）；智能分段预览位置不合理（应移入预览区 tab 切换）；缺少上传后自动分段功能 | 已修复 | 2026-02-23 |
| BUG-108 | 数据准备/PDF+DOCX | PDF/DOCX 文件导入后清洗阶段生成 0 segment 导致数据生成 code 1 崩溃。根因：环境配置仅安装 mlx-lm，未安装 PyPDF2/python-docx；清洗脚本 ImportError 静默跳过后生成脚本拿到空数据退出 | 已修复 | 2026-02-24 |
| BUG-109 | 数据准备/预览 | PDF/DOCX 文件点击预览显示乱码或报错。根因：read_file_content 直接 read_to_string 读取二进制文件；sample_raw_files snippet 也读取原始二进制字节导致模式检测失准 | 已修复 | 2026-02-24 |

## 回归记录

| 日期 | 版本 | 变更说明 | 结论 | 备注 |
| --- | --- | --- | --- | --- |
| 2026-02-27 | 0.4.8-hotfix-folder-ui | 修复导出页打开目录一致性（主按钮与 fused 按钮统一打开导出根目录）并统一 Export/Training/Testing/DataPrep 的“打开文件夹”按钮样式 | 代码自检通过 | TypeScript 编译通过；建议实机点按验证 Finder 打开路径 |
| 2026-02-09 | 0.1.0 | 初次记录 | 未通过 | 见 Bug 清单 |
| 2026-02-09 | 0.1.0-fix1 | 代码核对 BUG-001~003/006/007，新增 BUG-008/009 | 未通过 | 需实机回归 + 补充交互指引 |
| 2026-02-09 | 0.1.0-fix2 | 修复全部 9 个 BUG（BUG-001~009），编译通过，`pnpm tauri dev` 启动正常 | 代码自检通过 | 全链路代码走查无阻塞；需用户实机验证生成问答对功能 |
| 2026-02-09 | 0.1.0-fix3 | 重构数据集生成模块：多源支持(Ollama/内置规则)、模型自动检测下拉、ModelScope下载引导、手动添加模型路径、实时进度条、PRD更新 | 编译+DMG打包通过 | 需用户验证：1) Ollama模型检测 2) 内置生成效果 3) 下载链接可用性 |
| 2026-02-10 | 0.1.0-fix4 | 修复AI生成全部失败(think:false+2048token+双字段提取)；AI实时日志面板；停止/续传按钮；训练页模型下拉；增量保存 | 编译+DMG打包通过 | curl验证glm-4.7-flash加think:false后成功返回JSON；需用户实机验证完整生成流程 |
| 2026-02-10 | 0.1.0-fix5 | 修复环境检测横幅误导；mlx-lm内联检测(不依赖setup_env.py)；scripts打包进bundle；全局store持久化生成状态；caffeinate防睡眠；开源准备(README+LICENSE+.gitignore) | 编译+DMG打包通过 | 验证确认mlx-lm已安装(v0.30.6)但检测不到的根因是依赖未打包的外部脚本 |
| 2026-02-10 | 0.1.0-fix6 | 修复训练命令(mlx_lm lora --train)；训练进度可视化(Loss曲线图+迭代进度条)；中断清理(备份恢复机制)；打开文件夹按钮；清洗+生成合并为pipeline | 编译+DMG打包通过 | 训练命令根因是mlx-lm API变更(mlx_lm.lora→mlx_lm lora)且缺少--train标志 |
| 2026-02-10 | 0.1.0-fix7 | 删除确认+多来源模型选择+训练状态持久化+adapter管理+测试页面重构+推理temp参数 | 编译+DMG打包通过 | 9项修复 |
| 2026-02-10 | 0.1.0-fix8~fix12 | 模型选择全面重构(统一ModelSelector组件)、本地模型扫描(MLX标记)、在线自动下载、可折叠步骤+全宽日志、Ollama模型过滤、导出BUG修复 | 编译+DMG打包通过 | 训练页布局重构+统一模型选择器 |
| 2026-02-10 | 0.1.0-fix13~fix14 | 打开文件夹链接/模型验证BUG/tab切换/已训练适配器分类/导出页统一UI/设置页模型路径配置 | 编译+DMG打包通过 | UI细节优化 |
| 2026-02-10 | 0.1.0-fix15 | 导航重构：一级(控制台+项目)/二级(数据准备+训练+测试+导出)，自动加载最近项目，项目选中高亮 | 编译+DMG打包通过 | 导航体验改善 |
| 2026-02-10 | 0.1.0-fix16 | 测试/导出页UI优化：收起式下拉适配器+移除基础模型手动输入+自动带入+保存模型名称+字段顺序调整 | 编译+DMG打包通过 | 导出流程简化 |
| 2026-02-11 | 0.1.0-fix17 | 统一进度条+标题序号+项目页改造+导航重命名：StepProgress组件、1.1~4.3标题序号、空项目居中大按钮、删除确认弹窗、项目重命名、上传按钮移入1.1、恢复清洗到1.2、新增1.4生成类型、生成中时间轴高亮 | 编译+DMG打包通过 | 全面UI/UX改善，累计修复33个BUG |
| 2026-02-13 | 0.1.0-fix18 | 在线模型库重构：移除冷门模型，新增主流品牌分组（Qwen/DeepSeek/GLM/Llama/GPT-OSS/Gemma），训练与数据准备均支持按品牌展开选择版本，并补充“更多”模型外链 | 编译通过 | 在线选型更贴近主流热门模型 |
| 2026-02-13 | 0.1.0-fix19 | 在线模型二次校正：Qwen更新为Qwen3系列、GLM加入GLM-5、新增Kimi并移除Gemma，统一训练/HF与数据准备/Ollama在线预设 | 代码走查通过 | 已按用户反馈修正“版本落后/品牌不匹配”问题 |
| 2026-02-13 | 0.1.0-fix20 | 在线模型体验与来源策略对齐：发布时间排序、默认收折、补充Mistral/Phi、数据准备移除来源调整按钮；训练跳转设置可准确定位下载源模块；PRD同步当前完成度与优先级 | 代码走查+TypeScript编译通过 | 满足本轮9项需求并完成文档反向同步 |
| 2026-02-13 | 0.1.0-fix21 | 在线模型描述去除语言倾向（chineseGood→versatile等）；品牌分类行整行可点击展开（非仅文字区域） | TypeScript编译通过 | 描述客观化+交互热区扩大 |
| 2026-02-13 | 0.1.0-fix23 | 训练batch_size自动适配：启动前统计数据集行数，batch_size超过最小数据集大小时自动钳制，防止mlx-lm ValueError崩溃 | Rust编译通过 | 根因：valid.jsonl仅3条但batch_size=4，mlx-lm要求batch_size≤数据集大小 |
| 2026-02-13 | 0.1.0-fix22 | Python脚本全链路i18n：新建i18n模块+en/zh-CN语言包；重构6个脚本(clean_data/generate_dataset_ollama/generate_dataset_builtin/export_ollama/inference/download_model)所有日志和AI提示词改用翻译键；Rust 5个命令(start_cleaning/generate_dataset/export_to_ollama/start_inference/download_model)新增lang参数；前端3个页面+3个service传递当前语言 | TypeScript+Rust编译通过 | 可扩展设计：新增语言只需添加locales/xx.json，缺失键自动fallback到英文 |
| 2026-02-14 | 0.1.0-fix24 | 修复训练完成后 ETA 显示逻辑：完成态优先显示“已完成”，不再误显示“正在估算中...” | TypeScript+Vite 编译通过 | 最小改动，仅调整前端文案分支判断，无后端/训练逻辑变更 |
| 2026-02-14 | 0.1.0-fix25 | 推进 PRD 16.3：训练总结新增结构化导出（Markdown/CSV/SVG/PNG）、一键复制与系统分享入口 | TypeScript+Vite 编译通过 | 前端实现，无后端训练链路改动 |
| 2026-02-14 | 0.1.0-fix26 | 修复数据链路错用历史数据：生成前强制重清洗、训练强制选择数据集版本、后端移除dataset默认回退、Ollama生成增加语言一致性约束与脚本校验 | TypeScript+Vite 编译通过（Rust 环境本机缺少 cargo，未在此终端执行 cargo check） | 对应 BUG-041，根因覆盖“历史cleaned复用+训练隐式回退+跨语种漂移” |
| 2026-02-14 | 0.1.0-fix27 | 修复生成code2失败（脚本`--lang`兼容降级+stderr透传+更清晰错误文案）；开始生成自动滚动到顶部预览区 | TypeScript+Vite 编译通过 | stderr透传+--lang降级 |
| 2026-02-14 | 0.1.0-fix28 | 修复code2真正根因(stale closure空mode)；重构模式适配检测(后端内容采样+前端综合分析)；检测结果以图标形式显示在tab按钮右上角；智能默认选择推荐模式 | TypeScript+Vite 编译通过 | 对应 BUG-042(根因)/BUG-044/BUG-045 |
| 2026-02-15 | 0.4.0-fix29 | 修复训练页3项问题：停止后Loss空态文案、训练完成自动回顶、移除训练总结无效保存按钮（仅保留复制/系统分享） | TypeScript 编译通过 | 对应 BUG-046/BUG-047/BUG-048 |
| 2026-02-15 | 0.4.0-fix30 | 测试页体验重排：A/B改高级折叠、输入框增高、适配器默认收起、移除时间轴、补充A/B模式解释文案（投票UI隐藏但能力保留） | TypeScript 编译通过 | 对应 BUG-049/BUG-050/BUG-051/BUG-052/BUG-053 |
| 2026-02-15 | 0.4.0-fix31 | 修复数据准备智能分段预览串读历史数据：后端预览仅匹配当前 raw 文件 + 清洗产物写入 `segments_manifest.json` 做一致性校验 | Python 编译通过（Rust 本机缺少 cargo，需在有 cargo 环境复核） | 对应 BUG-054，导入新文件后不再展示旧分段预览 |
| 2026-02-15 | 0.4.0-fix32 | 修复 Markdown 单段样本 AI 生成失败：增强 Ollama JSON 兼容解析（中英键名归一化 + 非严格JSON提取），并在小样本批次放宽语言不一致拦截 | Python 编译通过 | 对应 BUG-055 |
| 2026-02-16 | 0.4.0-fix33 | 7 项数据准备页改进：日志语言跟随 UI(BUG-056)、数据隔离(BUG-057)、生成完成滚动到 1.4(BUG-058)、数据集可展开详情(BUG-059)、NEW 徽章(BUG-060)、分页(BUG-061)、右侧面板重命名+边框对齐(BUG-062) | Rust+TypeScript 编译通过 | 全部 7 项需求落地，无回归 |
| 2026-02-15 | 0.4.0-fix34 | 5 项修复：chat 模式截断 JSON 回收(BUG-063)、生成失败后恢复历史数据集(BUG-064)、train/valid 移入展开详情(BUG-065)、train/valid i18n 易懂化(BUG-066)、NEW 徽章纯文字+主题色(BUG-067) | TypeScript 编译通过 | 对应用户截图反馈 5 项 |
| 2026-02-19 | 0.4.1-fix35 | Ollama 导出与识别链路重构：以 daemon 运行态路径为单一真值，统一 `export_to_ollama`/`list_ollama_models`/`scan_local_models`/`open_model_cache`；`export_ollama.py` 支持 `--ollama-models-dir` 并回传 `manifest_dir`；导出成功区优先打开 manifest 目录；导出后模型列表自动刷新 | TypeScript 编译通过；Rust 本机缺少 cargo 未执行 `cargo check` | 对应 BUG-069，待用户按“默认路径+自定义路径”双场景实机回归 |
| 2026-02-20 | 0.4.1-fix36 | 适配器管理 3 项：移除数据准备/训练模型选择中的适配器分组(BUG-070)；导出 4.1 新增适配器删除按钮-两步确认防误删(BUG-071)；4.1「打开文件夹」改为打开 adapters/ 目录(BUG-072) | TypeScript 编译通过；Rust 新增 `delete_adapter` 命令含安全路径校验 | 待用户实机验证删除和打开文件夹行为 |
| 2026-02-20 | 0.4.1-fix37 | 3 项细节修复：i18n 补全删除按钮文字(BUG-073)；本地模型展开分组自动滚动(BUG-074)；4.1 适配器列表显示全部含无权重残留文件夹+删除全部入口(BUG-075) | TypeScript 编译通过 | 问题 4 结论：3 个无权重适配器为历史失败/中断训练的残留目录，非 BUG；现在均可在应用内删除 |
| 2026-02-20 | 0.4.1-fix38 | Ollama 路径管理补全：默认+自定义双路径扫描分组、设置页路径有效性提示与导出默认路径去硬编码、导出成功区路径明细与默认/自定义标识；修复导出“Step is still running”监听竞态 | TypeScript 编译通过；Rust 本机缺少 cargo 未执行 `cargo check` | 对应 BUG-076~079，待用户按自定义路径与导出链路实机回归 |
| 2026-02-20 | 0.4.1-fix39 | 修复导出模型 `unable to load model`：清理复用导出目录残留分片、去除重复张量并增加导出后运行态验证；修复 dataprep 模型可选性与 daemon 运行态不一致（BUG-080/081） | TypeScript+Python 编译通过；Rust 本机缺少 cargo 未执行 `cargo check` | 已通过本地复核：重复张量从 146 降为 0，`ollama create` 可成功；待用户按完整导出链路实机回归 |
| 2026-02-20 | 0.4.1-fix40 | fix40 回滚（已废弃）— 该版本尝试文件系统扫描补充 list_ollama_models，反而导致模型看似可选但实际 404 （BUG-082）。已在 fix41 回滚并正确处理 | 已回滚 | - |
| 2026-02-20 | 0.4.1-fix41 | 回滚 fix40，恢复 fix39 `list_ollama_models` daemon-only 行为；增加 HTTP 404 尓属处理：Python 语言生成脚本检测到 404 时 emit is_path_mismatch；前端显示黄色引导 banner，含示例 launchctl 命令 + 自动修复按钮（BUG-082/083）；新增 Tauri `fix_ollama_models_path` 命令 | Rust+TypeScript+Python 修改，待 cargo check | 路径错配置时用户可得到明确引导而非模糊错误 |
| 2026-02-20 | 0.4.1-fix42 | 修复 Ollama 路径一致性：导出优先配置路径（布局无效回退默认并告警）+ 导出前自动对齐 daemon；新增 `reset_ollama_models_path` 并接入设置页“恢复默认”，保证 effective path 真正回到默认（BUG-084/085） | Rust+TypeScript 修改，待 cargo check | 建议按“自定义有效/自定义无效/恢复默认”三场景做实机回归 |
| 2026-02-20 | 0.4.1-fix43 | 修复 BUG-084 根因：`resolve_ollama_models_dir_for_export` 改为检查路径可写性而非布局是否存在，新空目录现在直接使用（Python 脚本负责建立标准 manifests 结构）；修复 BUG-085 根因：`restart_ollama_app` 增加主动 `pkill ollama serve` + 等旧进程退出（最多 4s）+ 轮询等新 daemon 出现（最多 6s），避免旧进程残留导致生效路径刷新不及时 | Rust 修改，待 cargo check | 建议实机验证"全新空目录导出"和"恢复默认后立即查看生效路径"两场景 |
| 2026-02-20 | 0.4.1-fix44 | 修复 BUG-084 真正根因：通过截图确认用户设置的是"导出存储位置"（`export_path`），而非 `model_paths.ollama`；`export_to_ollama` 代码注释明确排除 `export_path` 导致完全无效；修改 `resolve_ollama_models_dir_for_export` 优先级为 `export_path` → `model_paths.ollama` → 默认路径 | Rust 修改，待 cargo check | BUG-085（恢复默认生效路径不更新）fix43 已修复（需重新编译）；建议按"设置导出存储位置→导出→验证路径"场景实机回归 |
| 2026-02-20 | 0.4.1-fix45 | 4 项修复：BUG-086 恢复默认后增加系统持久化 env 友好提示；BUG-087 分离 Ollama/GGUF 路径（回退 fix44，`export_path` 仅用于 GGUF，标签改为"GGUF 导出目录"）；BUG-088 "打开文件夹"改为优先打开 ollamaDir；BUG-089 Ollama/GGUF 导出互锁禁用 | TypeScript 修改，无 Rust 变更（export.rs 回退算 Rust 变更，待 cargo check） | 建议按"Ollama 自定义路径导出→打开文件夹"和"恢复默认→查看提示"场景实机回归 |
| 2026-02-20 | 0.4.1-fix46 | 修复 BUG-090：`scan_local_models` 改为 daemon 感知单路径扫描（删除双路径扫描逻辑）；`open_model_cache` 的 ollama 分支统一用 `resolve_ollama_models_dir()`；前端 ModelSelector 移除 `ollama_custom` source label/颜色/逻辑；i18n zh-CN/en 移除 `sourceLabels.ollama_custom` | Rust+TypeScript 修改，待 cargo check | 编译后本地模型列表只应出现单一 Ollama 分组，内容与 daemon 当前路径保持一致 |
| 2026-02-20 | 0.4.1-fix47 | 清理冗余与路径隐私：移除 `training.rs` 未使用导入、`export.rs` 未使用函数；将 `dataPrep` i18n、`download_model.py`、`USAGE.md` 中路径示例统一为 `/path/to/...`，避免个人绝对路径样式；补充一致性走查 | TypeScript + Rust 编译通过 | 关键路径逻辑保持不变，仅做安全清理与文案脱敏 |
| 2026-02-20 | 0.4.3 | 修复停止生成无效(BUG-092)、全部清除无确认弹窗(BUG-093)、停止后前端状态卡住(BUG-094)；新增生成队列视图（1.1 折叠+展开）、预览区下方统计栏（文件N/M·已生成·成功率）；移除重复进度条和状态栏；合并开关默认改为不勾选 | TypeScript + Rust 编译通过 | 深度测试待用户实机回归：1) 停止生成是否立即响应 2) 全部清除弹窗确认 3) 多文件生成时队列展示 4) 统计栏数据准确性 |
| 2026-02-21 | 0.4.3-fix48 | 修复 BUG-100：设置页“一键清理缓存”增加任务运行态互斥。训练/数据集生成/导出进行中时按钮禁用，并在点击处理增加前置拦截与提示文案 | TypeScript 编译通过 | 前端安全边界生效，避免任务执行中误触清理 |
| 2026-02-21 | 0.4.3-fix49 | 修复 BUG-101：控制台首页（Dashboard）增加“可清理缓存”卡片视图及前往清理快捷按钮；设置页关于模块版本号改为调用 Tauri `getVersion()` 动态获取，取代硬编码 | TypeScript 编译通过 | 首页容量透出提升用户感知，版本号动态化消除维护负担 |
| 2026-02-22 | 0.4.5 | 集群二数据质量提升：1.2 新增隐私过滤/模糊去重阈值/质量评分，1.4 新增质量角标与失败样本批量重试；后端与 Python 打通 quality/retry 参数并写入 quality.json + failed_segments.jsonl；修复重试误拦截与脚本参数兼容 | TypeScript + Python 编译通过（Rust 本机缺少 cargo，未执行 cargo check） | 对应 BUG-102/103，建议实机回归重试链路与质量展示 |
| 2026-02-22 | 0.4.5-fix50 | 修复跨项目状态残留与停止后预览未清空：DataPrep/Training 切换项目重置页面与 store 状态；异步加载增加 projectId 一致性校验防串线；停止后清空预览/日志区域 | TypeScript 编译通过 | 对应 BUG-104/105，建议按"停止后切项目"场景实机回归 |
| 2026-02-23 | 0.4.5-fix51 | 体系性跨项目隔离根治（BUG-106）：App.tsx 为 DataPrepPage/TrainingPage/TestingPage/ExportPage 加 key={projectId}，项目切换时强制完整重新挂载；同时在 App 层集中重置所有 store（generation/training/export/gguf/testing），彻底消除所有模块跨项目状态残留 | TypeScript 编译通过 | 建议按"任意页面停留→切换项目→核查每个业务页面"场景实机全面回归 |
| 2026-02-23 | 0.4.5 | 正式发布 0.4.5：根治跨项目隔离（BUG-107）；AppLayout Outlet 加 key={projectId} 强制重挂载；智能分段预览移入右侧预览区 tab 切换；新增智能分段可选勾选（默认勾选），上传文件后自动触发清洗/分段；清洗完成后自动切换到分段 tab；预览区文本内容字号放大（text-xs → text-sm + leading-loose）；分段项目标题/统计数字/正文分段字号全面放大并优化层级 | TypeScript 编译通过，版本号已同步 package.json/tauri.conf.json | 建议实机验证：1) 切换项目后各页面是否干净 2) 上传文件自动分段 3) tab 切换 4) 预览区字号是否清晰 |
| 2026-02-24 | 0.4.5-fix52 | 修复 PDF/DOCX 全链路支持（BUG-108/109）：自动补装 PyPDF2/python-docx；二进制文档文本提取；预览/清洗/生成全链路打通 | TypeScript + Python 编译通过 | 已回归 |
| 2026-02-24 | 0.4.6-fix54 | 修复导出找不到 ollama 命令（BUG-112）：三层修复——1) Rust `export_to_ollama` 通过 `resolve_ollama_bin_path` 获取完整路径并传 `--ollama-bin` 给 Python；2) Python `export_ollama.py` 新增 `resolve_ollama_bin()` 智能搜索（hint→常见路径→which→裸名）；3) 设置页新增 Ollama 程序路径配置项，自动检测+手动兜底 | Rust + TypeScript 编译通过 | 建议实机验证：1) 导出到 Ollama 是否正常 2) 设置页 Ollama Binary 是否显示检测到的路径 3) 手动更改路径后导出是否生效 |
| 2026-02-24 | 0.4.5-fix53 | 修复数据预览自动展开 + 多语言 prompt 智能匹配（BUG-110/111）：1) 根治数据预览框收折问题：移除导致内容渲染被卸载的冗余条件 `&& previewTab !== "segment"`，并在所有触发生成的入口（`startGenerationStep` 和 `handleRetryFailed`）显式调用 `setPreviewTab("data")` 确保状态同步，避免因异步和事件监听造成的状态竞态；2) 实现方案D——i18n 双语境架构：`t()` 用于日志（跟随 UI 语言），`pt()` 用于 LLM prompt/模板（跟随内容语言）；3) 新增 `detect_content_language()` 自动检测源文本主导语言（支持 CJK/Latin/Cyrillic/Arabic/Hangul/Kana），匹配对应 locale 文件的 prompt 模板，无匹配 locale 时回退英文；4) `generate_dataset_ollama.py` 和 `generate_dataset_builtin.py` 均已适配，prompt 用 `pt()`、日志用 `t()`；5) 检测结果通过日志输出告知用户；6) 架构为后续 es/fr/pt/it/de/ja/ko/ru/zh-tw 等多语种扩展预留，只需新增 locale JSON 文件即可生效 | TypeScript + Python 语法验证通过 | 建议实机验证：1) 开始生成后，无论当前处于什么 Tab 或是重试场景，右侧数据预览框是否始终保持展开和可见状态 2) 中文 UI + 英文 PDF 生成是否不再报语言不匹配 3) 日志是否显示检测到的内容语言 |
| 2026-02-24 | 0.4.6-fix55 | 补齐 BUG-112 的 4 处统一：`check_ollama_status`、`list_ollama_models`、`verify_export_model`、`get_app_config` 全部统一走配置解析后的 Ollama Binary（含可执行性判断），避免导出链路与状态链路不一致 | 待实机回归（本轮完成代码走查） | 建议按“自定义 Ollama Binary 路径”场景回归：状态显示、导出按钮可用性、导出后 verify、模型列表一致性 |
| 2026-02-25 | 0.4.6-fix56 | 修复 BUG-113：Ollama 不支持 `Qwen3ForCausalLM` 架构时用户体验差问题。1) 新增 `_read_model_arch()` 在导出开始时读取 `config.json` 展示架构名；2) 新增 `_strip_ansi()` 清除 stderr 中的终端控制序列；3) 新增 `_interpret_ollama_stderr()` 识别 `unsupported architecture` 等已知错误模式，替换为友好提示并附升级引导链接；4) 同步更新 `locales/en.json` 和 `locales/zh-CN.json` 新增 `export.arch_detected` / `export.ollama_error_unsupported_arch` 两个 i18n key | Python 语法验证通过（py_compile） | 建议回归：1) 使用 Qwen3 模型导出，确认日志中有架构检测行 2) 导出失败时看到的是友好提示而非乱码 stderr 3) 提示语中包含 Ollama 升级链接 |
